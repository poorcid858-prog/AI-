/**
 * 并行 4 session 实测骨架 — 每个 session 认领一个任务包，检查能否独立推进
 *
 * 四个并行 agent，各读自己的任务包文档，各自只碰自己的文件，
 * 各自在进展.md 记录（会并发写——用来验证并发安全）。
 *
 * 注意：这是实测。agent 跑完后检查进展.md 是否 4 条记录都在且没互相覆盖。
 */

// 不实际启动服务器 —— 任务包 A（浏览器验收）和 B（删数据）会改动真实数据，
// 这里只验证"每个 session 能否从文档独立理解任务 + 遵守 superpowers + 文件隔离"。
console.log('并行 session 骨架确认：文档就绪，规则明确，可实测');
console.log('真正并行执行时需要：A 浏览器人工验收 / B 需 PM 决定 / C·D 可自动跑');