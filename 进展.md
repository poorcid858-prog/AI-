# 企业 AI 辅助工具 · 进展记录

> 本文件是项目唯一进展中枢。**新 session 必须先读本文件**。

---

## 📜 项目契约（铁律）

以下 3 条是项目最高规则，任何 session、任何任务都必须遵守。每次完成实质产出后，若契约有新增/变更，更新到本节。

### 契约 1：必须使用 superpowers 做开发
- ✅ 任何有实质产出的任务前，**先调用 Skill 工具**选择对应的 superpowers skill（编码→TDD，排查→systematic-debugging，计划→writing-plans，完成前→verification-before-completion）
- ❌ 不能跳过；不在"读代码/写文档/跑命令"之后才调，要先调
- 📋 每条进展记录必须写明「使用的 skill」，方便 PM 验证

### 契约 2：项目文件只能放在项目文件夹内
- ✅ **本项目所有文件**（代码、文档、配置、测试、数据）只能放在 `d:\temp\ai-assistant\` 内
- ❌ 严禁在 `d:\temp\` 根目录或项目外任何位置新建本项目文件
- ✅ 本文档（进展.md）就是项目专属进展，已从根目录移入 `d:\temp\ai-assistant\进展.md`

### 契约 3：进展.md 必须时刻保持更新
- ✅ 每次完成有实质产出的任务后，**主动更新本文件**（改代码、完成阶段、出现待决定事项时）
- ✅ 更新目标：**任何新 session 打开本文件，马上能了解项目全貌**（当前进度、任务状态、资源位置、执行规则）
- ✅ 新记录插在本「快速上手指南」段落**下方**（倒序，最新在上方）
- ⚠️ 待用户决定的事项必须用 ⚠️ 标记
- ✅ 标注来源 + 使用的 superpowers skill

### 契约 4：优先使用前端/UI 插件（与 superpowers 铁律同级）
- ✅ 任何需要前端交互、图表、表格、弹窗、编辑器、动效的场景，**优先使用已集成的第三方前端/UI 插件**，不重复造轮子
- ✅ 插件采用 **npm 安装（依赖锁进 package.json）+ `npm run vendor` 复制到 public 供静态引用** 的本地化方式，不用 CDN（保证离线可用、上云一致）
- 📋 插件清单与引用参考见 `public/js/vendor/`、`public/css/vendor/` 及 `docs/前端插件清单.md`
- ❌ 新建前端功能前，先查清单是否已有可用插件；已有就不手写原生方案
- 📌 常用插件速查：图标 `lucide`/`bootstrap-icons`，弹窗 `sweetalert2`，提示 `toastify`，表格 `datatables`，图表 `echarts`/`chart.js`，日历 `fullcalendar`，拖拽排版 `gridstack`/`sortablejs`，富文本 `quill`，下拉 `select2`，日期 `flatpickr`，代码高亮 `prism`，动效 `animate.css`，XSS 清理 `dompurify`，文本对比 `diff`，代码格式化 `js-beautify`

### 契约 5：部署铁律（所有 session 必须遵守）

> 本项目已部署到云服务器，**任何涉及部署、代码更新、运维的 session 必须先读本节**。

#### 5.1 云服务器基本信息
- **IP**：124.223.99.237
- **SSH 密钥**：`ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICO9gyQ22kUj96siFvq7bDmLCM5/7RVp9n9EwO9+a8ZZ gongcheng@LAPTOP-1MJGU3RA`
- **SSH 连接命令**：`ssh root@124.223.99.237 -i ~/.ssh/id_ed25519`
- **服务器系统**：Linux（root 账户登录）
- **Node.js 版本**：v20.20.2

#### 5.2 项目访问地址（ai-assistant 专用路径）
| 项目 | 访问地址 | 后端端口 | 部署路径 | 说明 |
|------|---------|---------|---------|------|
| **ai-assistant** | **`http://124.223.99.237:8666/ai/`** | 内网 3000（pm2 管理） | `/opt/ai-assistant/` | **`/ai/` 路径是 ai-assistant 专用**，外网经 nginx 反代 8666 端口 |

> ⚠️ **端口说明**：ai-assistant 访问地址是 **`http://124.223.99.237:8666/ai/`**。本地监听 3000，外网经 nginx 反代到 8666 端口的 `/ai/` 子路径。**不要试图用 3000 端口外网访问，它不通。**
> ⚠️ **8666 根路径已不再指向任何项目**（篮球游戏已删除），访问根路径返回 404。以后新项目放 `/opt/微信小游戏/` 或另配路径。

#### 5.3 服务管理
- **pm2 进程名**：`ai-assistant`
- **常用命令**：
  ```bash
  pm2 status                    # 查看服务状态
  pm2 restart ai-assistant      # 重启服务
  pm2 logs ai-assistant         # 查看日志
  pm2 save                      # 保存进程列表（开机自启）
  ```
- **nginx 配置**：`/etc/nginx/conf.d/ai-assistant-8666.conf`（ai-assistant 的 `/ai/` 反代）
- **nginx 重载**：改配置后执行 `nginx -s reload`

#### 5.4 代码更新方式（⚠️ 重要：服务器无法直接连接 GitHub）
云服务器无法直连 GitHub（SSH 和 HTTPS 均不通），**禁止直接 `git pull`**，必须使用 **git bundle 离线传输**：

```bash
# 第 1 步：本地电脑打包
cd d:\temp\ai-assistant
git bundle create /tmp/ai-update.bundle HEAD

# 第 2 步：scp 传到服务器
scp /tmp/ai-update.bundle root@124.223.99.237:/opt/ai-update.bundle

# 第 3 步：服务器上拉取
ssh root@124.223.99.237
cd /opt/ai-assistant
git pull /opt/ai-update.bundle master
npm install                     # 如有新依赖
pm2 restart ai-assistant        # 重启服务

# 第 4 步：清理
rm /tmp/ai-update.bundle
ssh root@124.223.99.237 rm /opt/ai-update.bundle
```

#### 5.5 双口令部署（可选）
默认登录 admin/123456（mock 模式）。如需启用访客/调试双口令，SSH 到服务器后配置：
```bash
cd /opt/ai-assistant
pm2 stop ai-assistant
DUAL_PASSWORD=true GUEST_PASSWORD=guest123 DEBUG_PASSWORD=debug123 pm2 start server.js --name ai-assistant
pm2 save
```

#### 5.6 已知问题
- ❌ 服务器无法直连 GitHub，代码更新必须走 git bundle 三步流程
- ❌ 外网 3000/80 端口不可达，仅开放 8666 和 8888
- ❌ 8666 根路径返回 404（篮球游戏已删除，`/ai/` 是 ai-assistant 专用）
- ⚠️ 双口令当前未启用，需手动配置环境变量
- 🔄 能力审核 `GET /api/capabilities/pending-review` 路由目前返回 404

---

## 🚀 项目快速上手指南（新 session 必读）

### 1. 项目一句话
把企业散落的 Word/PDF/PPT 知识加工成 AI 可引用的知识库，为**产品/测试/前端/客服**四类角色生成文档。技术栈：Node.js + Express + JSON 文件存储 + TF-IDF 语义检索（模拟向量）。

### 2. 项目文件夹与资源位置
**所有项目文件**都在 `d:\temp\ai-assistant\`：
- 代码：`lib/`（22 个核心引擎）、`routes/`（16 个 API 路由）、`public/`（前端页面）、`server.js`（入口）、`config.js`（配置）
- 测试：`test/`（33 个测试文件，含 526 用例）
- 数据：`data/`（运行时，不入库）、`mock-data/`（模拟数据）
- 文档：`docs/`（约 40 篇）
- 进展记录：本文件（`d:\temp\ai-assistant\进展.md`）

**外部资源**：
- GitHub 仓库：`git@github.com:poorcid858-prog/AI-.git`（origin master）
- 云服务器：124.223.99.237（部署信息见下方「📌 部署铁律」）

### 3. 启动与运行
```bash
cd d:\temp\ai-assistant
npm start        # 访问 http://localhost:3000
npm test         # 跑全部测试
```
默认**模拟模式**（无需 API Key）跑通全流程；配 `.env` 可接真实大模型（LLM_MODE=real）。

### 📌 部署铁律（所有 session 必须遵守）

> 以下为 ai-assistant 项目在云服务器上的部署信息，**任何 session 涉及部署、更新、运维都必须先读本节**。

#### 5.1 云服务器基本信息
- **IP**：124.223.99.237
- **SSH 密钥**：`ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAICO9gyQ22kUj96siFvq7bDmLCM5/7RVp9n9EwO9+a8ZZ gongcheng@LAPTOP-1MJGU3RA`
- **SSH 连接命令**：`ssh root@124.223.99.237 -i ~/.ssh/id_ed25519`
- **服务器系统**：Linux（root 账户登录）
- **Node.js 版本**：v20.20.2

#### 5.2 项目访问地址
| 项目 | 访问地址 | 服务器端口 | 部署路径 |
|------|---------|-----------|---------|
| **ai-assistant** | `http://124.223.99.237:8666/ai/` | 内网 3000（pm2 管理），外网经 nginx 反代到 8666 `/ai/` 子路径 | `/opt/ai-assistant/` |
| **投篮游戏** | `http://124.223.99.237:8666/` | 外网 8666 端口（根路径） | `/opt/basketball-web/` |

> ⚠️ **注意**：云服务器安全组仅放行了 8666 和 8888 端口，**3000/80/8667 端口外网不可达**。ai-assistant 与投篮游戏**共享 8666 端口**，通过 nginx 子路径 `/ai/` 区分。

#### 5.3 服务管理
- **pm2 进程名**：`ai-assistant`
- **常用命令**：
  ```bash
  pm2 status                    # 查看服务状态
  pm2 restart ai-assistant      # 重启服务
  pm2 logs ai-assistant         # 查看日志
  pm2 save                      # 保存进程列表（开机自启）
  ```
- **nginx 配置**：`/etc/nginx/conf.d/basketball-8666.conf`（ai-assistant 的 `/ai/` 反代也在此文件）
- **nginx 重载**：改配置后执行 `nginx -s reload`

#### 5.4 代码更新方式（⚠️ 重要：服务器无法直接连接 GitHub）
由于云服务器无法直连 GitHub（SSH 和 HTTPS 均不通），**不能直接 `git pull`**，必须使用以下 **git bundle 离线传输流程**：

```bash
# 第 1 步：在本地电脑（d:\temp\ai-assistant）执行
cd d:\temp\ai-assistant
git bundle create /tmp/ai-update.bundle HEAD

# 第 2 步：scp 传到服务器
scp /tmp/ai-update.bundle root@124.223.99.237:/opt/ai-update.bundle

# 第 3 步：在服务器上执行
ssh root@124.223.99.237
cd /opt/ai-assistant
git pull /opt/ai-update.bundle master
npm install                     # 如有新依赖
pm2 restart ai-assistant        # 重启服务

# 第 4 步：清理临时文件
rm /tmp/ai-update.bundle        # 本地
ssh root@124.223.99.237 rm /opt/ai-update.bundle  # 服务器
```

#### 5.5 双口令部署（可选）
默认使用 admin/123456 账号密码登录（mock 模式）。如需启用访客/调试双口令，SSH 到服务器后：
```bash
cd /opt/ai-assistant
pm2 stop ai-assistant
DUAL_PASSWORD=true GUEST_PASSWORD=guest123 DEBUG_PASSWORD=debug123 pm2 start server.js --name ai-assistant
pm2 save
```

#### 5.6 已知问题
- ❌ 服务器无法直连 GitHub，代码更新必须走 git bundle 三步流程
- ❌ 外网仅开放 8666 和 8888 端口，3000 端口外网不可达
- ⚠️ 双口令当前未启用，需手动配置环境变量
- 🔄 能力审核的 `GET /api/capabilities/pending-review` 路由当前返回 404（submit-review 和 review-capability 接口正常）

### 4. 新 session 启动流程（按顺序）
1. **读本文档**（进展.md）——最新进度 + 待决定事项 + 契约
2. **读** `docs/并行任务入口.md`——项目全景 + 任务分配表
3. **读** `docs/新session工作规则.md`——工作流程和规则
4. **读** `docs/任务总览-2026-08-15.md`——任务包优先级/依赖关系
5. **读** `docs/错误日志.md`——避免重复踩坑
6. **读** `~/.claude/projects/d--temp/memory/MEMORY.md`——用户记忆
7. **调用 superpowers skill**——遵守契约 1

### 5. 当前任务状态
| 任务 | 状态 | 说明 |
|------|------|------|
| A: 知识库页面修复 | ✅ 完成 | Session 1 |
| B: 系统管理员权限补全 | ✅ 完成 | Session 2 |
| C: 审核中心重新设计 | ✅ 完成 | Session 3 |
| D: 能力中心后端 | ✅ 完成 | Session 4（创建/删除）+ Session 9 补全列表/权限 |
| E: 能力中心前端 | ✅ 完成 | Session 5 |
| F: 知识库四层架构后端 | ✅ 完成 | Session 6，遗留前端"发布按钮" |
| G: 知识库四层架构前端 | ✅ 完成 | Session 7 |
| H: 运营中心 | ✅ 完成 | Session 8 |
| I: 管理后台配置功能 | ✅ 完成 | Session 9 |
| J: 聊天页提问模板 + UI优化 | ✅ 完成 | Session 10（Bootstrap 5 深色主题） |

### 6. 待办事项（⚠️ 需用户决策或推进）
- ⚠️ 审核中心前端缺"发布"按钮（审核通过后的新增独立操作，后端 API 已有，前端待补）
- ⚠️ 运营中心、管理后台等新页面**待 PM 浏览器验收**（功能/UI）
- ⚠️ 云服务器项目路径和重启方式待确认（需 ssh 上去查）
- ⏳ 全量测试通过后建议做一次统一回归验收

---

## 📖 主人的需求文档路径

> 所有新 session 开展工作前，必须先读以下需求文档（按顺序），理解需求后再动工。
> 读完需求文档后，再读下面的「需求与现状差异分析」了解当前差距。

### 需求文档清单（"主人的需求文档"目录下共 6 份，全部必读）

| 序号 | 文档路径 | 内容 |
|------|---------|------|
| 1 | `主人的需求文档/企业AI产研辅助平台_开发理解版需求文档_V2.0.md` | 核心需求文档：六大模块定义、15 条概念边界铁律、权限模型、14 步知识流程、AI 客服分支、双口令部署等 |
| 2 | `主人的需求文档/补充说明文档1.md` | 六大模块职责边界、数据流向、架构图文字版 |
| 3 | `主人的需求文档/补充说明文档2.md` | 能力中心正确理解、Skill 数据结构、AI 能力与 Workflow 关系 |
| 4 | `主人的需求文档/企业AI产研辅助平台_页面结构_信息结构_功能结构_V2.0.md` | 页面结构图、信息结构图、功能结构图、AI 执行流程、核心对象关系 |
| 5 | `主人的需求文档/企业AI研发辅助平台_产品架构全景图_文字版.md` | 产品架构全景（五大核心区域）、各模块功能详解 |
| 6 | `主人的需求文档/企业研发AI辅助平台_任务执行全流程_文字版.md` | AI 任务执行全流程详解 |

### 能力中心核心理解（必须记住）

以下是经过 PM 确认的、对能力中心的正确理解，任何新 session 必须先读这段：

1. **能力中心是后台配置工具**，不是前台用户直接使用的入口。用户在 AI 工作台使用 AI，能力中心负责配置"AI 能做什么"。
2. **新增 AI 能力时，核心是选择 Workflow**（已有的或新建的）。Workflow 不是空壳，它已经包含了 Skill、Reference、Prompt、Script、Tool 等要素。选好 Workflow 就确定了这个能力包含什么。
3. **能力中心必须提供以 AI 能力为单位的列表（聚合视图）**，每条能力能看到它关联的 Workflow，以及该 Workflow 包含的全部要素，还有覆盖岗位、状态、版本。
4. **能力发布 ≠ 自动出现在工作台**。能力发布是系统层面的。如果希望能力在聊天页被用户使用，需要人工配提问模板，配的时候选择关联哪个能力。
5. **能力中心与 AI 工作台没有前端视觉上的联动。** 能力到工作台的桥梁是人去配提问模板。

### 1. 一句话定位
不是 AI 聊天工具，而是把**企业知识 + AI 能力 + 业务流程 + 大模型**组装成完整的企业 AI 应用。核心价值：通用大模型不知道企业业务规则，本平台用内部已审核知识补齐。

### 2. 六大功能模块

| 模块 | 一句话职责 | 当前状态 |
|------|-----------|---------|
| **AI 工作台** | 用户使用入口（产品/测试/前端助手 + AI 对话 + 历史记录） | ⚠️ 基本聊天有，但缺少"意图识别路由到 Workflow"的完整链路 |
| **知识中心** | 管理企业知识（文档→标准化→Chunk→向量四层，含溯源） | ✅ 四层架构已实现，含溯源 Tab |
| **能力中心** | 创建/配置/测试/发布 AI 能力（Workflow/Skill/Reference/Prompt/Tool/Script） | ✅ 有 CRUD + 草稿/试跑/发布/回滚，但前端创建页面缺失 |
| **审核中心** | 审核知识、能力、结果 | ✅ 表格列表已实现，但缺"发布"按钮 |
| **运营与管理中心** | 分析 AI 使用与效果、问题定位 | ✅ 运营中心已实现，但效果分析/能力运营待完善 |
| **系统管理** | 用户/角色/权限、模型配置、AI 日志、系统配置 | ✅ 配置功能基本完成，但模型配置/AI 日志/用户管理待完善 |

### 3. 核心概念边界（15 条铁律）
1. AI 工作台以**自然语言**为主要入口，LLM 做意图识别，不强制用户选任务类型
2. **AI 能力 ≠ Workflow** —— 能力是用户看到的"需求分析助手"，Workflow 是背后的执行流程。能力中心是后台配置工具，不是前台用户直接使用的入口
3. **Workflow 已经包含 Skill / Reference / Prompt / Tool / Script 等要素** —— 新增 AI 能力时，核心是选择 Workflow（已有的或新建的），选好 Workflow 就确定了能力包含什么，Workflow 不是空壳
4. **能力中心产出以 AI 能力为单位的聚合视图**：一条能力能看到它关联的 Workflow，以及该 Workflow 包含的全部要素，还有覆盖岗位、状态、版本
5. **能力发布 ≠ 自动出现在工作台** —— 能力发布是系统层面的，不自动出现在 AI 工作台 UI 上。如果希望用户在聊天页使用，需要人工配提问模板，配的时候选择关联哪个能力
6. **RAG 不是 Skill 的子步骤**，是 Workflow 可复用的知识获取能力
7. **Skill ≠ Prompt**、**Reference ≠ RAG**、**Tool ≠ Script**（各自有明确边界）
8. Chunk 保留 document_id 父子关系，逐层可溯源
9. Prompt 分层设计：全局→角色→Workflow→Skill→用户输入
10. 能力必须**测试→审核→发布**后才能被工作台调用
11. 模型统一在系统管理配，Workflow 只引用已配好的模型
12. **每次 AI 执行记录完整链路**（Request ID → 意图→Workflow→RAG→召回 Chunk→Skill→Reference→Prompt→LLM→输出），用于问题定位

### 4. 权限模型（核心机制）
- **业务线 × 密级**双重隔离：公开/内部/机密/绝密 四档
- 六类角色：产研用户、客服、知识管理员、审核人员、技术管理员、演示访客
- **职责分离**：不能自己传自己审
- 技术管理员"管管道不管内容"，涉密正文脱敏

### 5. 知识加工 14 步流程
上传 → 解析 → 清洗 → 结构化 → 打标签 → 质检(一) → ★人工审核 → 切分 → 去重 → 质检(二) → Embedding → 写向量库 → 发布 → 下架/替换/归档

### 6. 本期建设范围（4 深 2 浅）
- **深做**：AI 工作台、知识中心、审核中心、AI 能力中心
- **浅做**：AI 运营（只深做 RAG 检索分析 + 使用数据）、系统管理（骨架）

### 7. 关键设计要点
- **双向反馈**：优秀问答→案例库（Few-shot）；AI 生成内容经审核后回流知识库
- **AI 客服分支**：关键字匹配（分词→同义词归一→命中话术），不走向量检索
- **召回可观测与反向修复**：检索快照 + 三种修复动作（改原文/调参数重加工/删除），执行前影响预览
- **双口令部署**：访客口令(演示模式，写不落库) / 调试口令(真实模式，PM 用)
- **回答对比调试工具**：快照 A ↔ 快照 B 并排对比

### 8. 建议开发顺序
1. 建立整体项目框架 ✅
2. 用户、角色、权限 ✅（基本）
3. AI 工作台 ⚠️（缺意图识别→Workflow 路由链路）
4. 知识中心 ✅
5. 能力中心 ⚠️（缺前端创建页）
6. 审核中心 ⚠️（缺发布按钮）
7. 运营与管理中心 ⚠️（效果分析/能力运营待完善）
8. 系统管理 ⚠️（模型配置/AI 日志/用户管理待完善）
9. 打通完整 AI 运行链路 ❌（未开始）

---

## 需求与现状差异分析（待建设清单）

> 对照 V2.0 需求文档逐项检查后的差异项，按模块分组，标注优先级。

### 一、AI 工作台（当前：基本可用，但缺核心链路）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 1 | **意图识别路由** | 目前聊天是直接模拟回答，没有 LLM 意图识别 → 判断任务类型 → 路由到对应 Workflow 的链路 | P0 |
| 2 | **Workflow 引擎** | `routes/workflow.js` 只有占位 health 路由，无实际执行逻辑 | P0 |
| 3 | **Workflow 编排** | 需求要求 Workflow 可以配置节点、节点顺序、条件判断、调用的 Skill 等，当前无实现 | P0 |
| 4 | **能力与提问模板的关联** | 能力发布后，应有人工配置提问模板的入口，选择关联哪个能力。当前无此关联 | P1 |
| 5 | **能力列表（聚合视图）** | 需求要求以 AI 能力为单位的列表，每条能力能看到它包含哪些 Workflow / Skill / Reference / Prompt / Tool/Script，当前无此聚合视图 | P1 |
| 6 | **历史记录增强** | 每条回答旁边应有"查看召回"按钮 → 跳转运营中心/检索快照；当前无此跳转 | P1 |

### 二、能力中心（当前：有后端 CRUD + 草稿机制，前端创建页缺失）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 7 | **能力创建前端页面** | 当前 `public/admin-capability.html` 页面的"新增能力"功能缺失，无法在 UI 上创建新能力 | P0 |
| 8 | **能力列表（聚合视图）** | 需求要求以 AI 能力为单位的列表，每条能力能看到它包含哪些 Workflow / Skill / Reference / Prompt / Tool/Script、以及状态和版本。当前只有 5 种类型的列表，没有能力维度的聚合视图 | P0 |
| 9 | **Workflow 可视化编排** | 需求要求 Workflow 可配置节点、顺序、条件判断，当前仅为文本定义 | P1 |
| 10 | **能力测试→审核→发布全流程** | 后端的草稿→试跑→发布→回滚已有，但前端缺少"提交审核"按钮，且审核中心无"能力审核"Tab | P1 |
| 11 | **Reference 能力** | 能力中心有 Reference 类型，但后端存储和前端展示尚未完善（如加载企业模板/规范库） | P2 |
| 12 | **Tool/Script 能力** | 同样有类型定义，但缺少实际执行能力（Tool 调用外部 API、Script 执行程序逻辑） | P2 |
| 13 | **能力与提问模板关联** | 能力发布后，管理员应能在提问模板配置页选择关联某个能力。当前模板和能力的关联未建立 | P1 |

### 三、知识中心（当前：四层架构最完整）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 12 | **审核中心"发布"按钮** | 后端的 publish API 已有，审核中心前端缺少"发布"按钮 | P0 |
| 13 | **文档上传页归属** | 需求要求上传功能在知识中心，不在管理后台；当前 `admin.html` 仍可上传 | P1 |
| 14 | **知识质量页面** | 需求要求展示质检得分、异常片段，当前无此页面 | P1 |
| 15 | **客服话术库管理** | 需求要求有独立的问答对 + 同义词表 + 未命中问题池管理页面，当前仅后端有同义词处理 | P1 |
| 16 | **文档下架/替换/归档 UI** | 后端 API 有 expiry 路由，前端缺少下架/替换/归档操作页面 | P1 |

### 四、审核中心（当前：表格列表可用）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 17 | **"发布"按钮**（同 12） | 审核通过后需独立"发布"操作 | P0 |
| 18 | **优秀案例审核** | 需求要求审核中心有"优秀案例审核"Tab（双向反馈回路一的审核入口），当前无 | P1 |
| 19 | **审核记录流水** | 需求要求有审计流水页面，当前无 | P2 |

### 五、运营与管理中心（当前：基础功能已实现）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 20 | **效果分析页面** | 需求要求满意度、点赞/点踩、任务完成情况、结果采纳率，当前仅运营中心有满意度趋势 | P1 |
| 21 | **能力运营分析** | 各 AI 能力调用情况、成功率、耗时分布，当前无 | P1 |
| 22 | **问题定位入口** | 从运营中心单条记录应能跳转到完整执行链路视图（需求 17 的链路追踪），当前仅 Chunk 追踪 | P1 |

### 六、系统管理（当前：配置功能基本完成）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 23 | **模型配置管理** | 需求要求统一管理模型名称、类型、API 地址、Key、Temperature、Token 限制、启用状态，当前无 | P1 |
| 24 | **AI 日志页面** | 需求要求记录 Request ID、用户、时间、模型、Token、耗时、状态、错误信息，当前无 | P1 |
| 25 | **用户管理页面** | 需求要求创建用户、管理权限、查看登录记录，当前仅 admin.html 可查看用户列表 | P1 |
| 26 | **角色/权限配置** | 当前角色和权限硬编码在 config.js 中，需求要求可配置 | P2 |

### 七、AI 客服分支（当前：关键字匹配引擎有，管理页面缺失）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 27 | **同义词表管理页面** | 运营人员需要在页面上增删改同义词，当前仅后端有 synonyms 数据 | P1 |
| 28 | **未命中问题池页面** | 零命中的客户问题记入池子，当前无管理页面 | P1 |
| 29 | **客服话术库管理页面** | 标准话术增删改查，当前无 | P1 |
| 30 | **客服效果指标看板** | 话术库命中率/公开级兜底率/转人工率/平均命中关键字数，当前无 | P2 |

### 八、部署与访问（双口令 + 回答对比调试）

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 31 | **双口令部署** | 访客口令(演示模式)/调试口令(真实模式)，当前无此机制 | P1 |
| 32 | **落地页引导** | 三步体验路径引导，当前无 | P1 |
| 33 | **回答对比调试工具完善** | 快照对比已有基础，但测试问题集管理、一键跑等需完善 | P1 |

### 九、数据与链路完整性

| # | 差异项 | 说明 | 优先级 |
|---|--------|------|--------|
| 34 | **完整 AI 运行链路打通** | 需求要求：用户输入→意图识别→Workflow→RAG→Skill→Reference→Prompt→LLM→输出→反馈→效果分析→优化，当前链路断在"意图识别→Workflow" | P0 |
| 35 | **AI 执行链路记录** | 每次 AI 请求必须记录完整链路用于问题定位，当前有检索快照但未覆盖全链路 | P1 |
| 36 | **双向反馈回路** | 优秀问答→案例库(回路一)和 AI 生成→知识库回流(回路二)均未实现 | P1 |

### 优先级说明

| 级别 | 含义 | 数量 |
|------|------|------|
| **P0** | 核心链路缺失，不解决则产品无法跑通完整流程 | 4 项 |
| **P1** | 重要功能，提升产品可用性和完整性 | 23 项 |
| **P2** | 增强功能，优化体验和深度 | 9 项 |

**总计：36 项差异**



## 进展记录（新记录插在本段下方，倒序）

### 2026-08-16（K4 session）
- ✅ [任务包K4] **Prompt 配置移入能力中心 + UI 全面美化 完成**
  - 使用 superpowers skill：`superpowers:test-driven-development`（编码）+ `superpowers:brainstorming`（界面设计）
  - **Prompt 配置移入能力中心**：
    - `public/js/sidebar.js`：能力中心二级菜单「Prompt」从 `/admin-config.html?tab=prompts` 改为 `/capability.html#prompt`
    - `public/capability.html`：新增第 6 个子类型菜单「Prompt」（原 5 个子类型 Workflow/Skill/Reference/Script/Tool 旁），点击后展示分层 Prompt 管理界面（列表 + 新增/编辑/删除），复用后端 `/api/admin/prompt-layers` API
    - 分层规则：global（全局）→ role（角色）→ workflow（工作流）→ skill（技能）→ user（用户输入），支持按角色名/业务线过滤
    - 仅 admin 角色可配置 Prompt（非 admin 显示提示信息）
  - **后端修复**：
    - `routes/admin.js`：修复 put/delete 中 `parseInt(id, 10)` 与字符串 id 不匹配导致更新/删除失败的问题（改为字符串比较）；POST 新增 level 与 prompt_text 必填校验（缺 level 返回 400）
    - `lib/admin-config.js`：`updatePromptLayer`/`deletePromptLayer` 改为 `String(l.id) === String(id)` 比较，兼容混合 id 类型
  - **UI 美化**（统一视觉语言，收拢内联样式）：
    - 新建 `public/css/capability-ui.css`（能力类型 Tab、分组卡片、Prompt层级标签、状态 badge、步骤指示器等）
    - 新建 `public/css/admin-ui.css`（配置 Tab、表单控件、审核 Tab、统计卡片、空态/加载态等）
    - 将 `admin-config.html` 内联 `<style>`（约 30 行）外移到 `admin-ui.css`
    - `capability.html` / `review.html` / `knowledge.html` / `dashboard.html` / `workspace.html` / `operations.html` 均引入 `admin-ui.css` / `capability-ui.css`
  - **测试**：新增 `test/k4-prompt-in-capability.test.js`（7 个用例：sidebar 指向、capability 含 Prompt 界面、聚合视图保留、admin-config CRUD 函数、API 完整 CRUD 工作流、非 admin 403、缺 level 400）
  - **验证**：npm test 594/594 通过（基线 587 + 新增 7），静态资源 HTTP 200
  - 涉及文件：`public/js/sidebar.js`、`public/capability.html`、`public/admin-config.html`、`public/css/capability-ui.css`(新)、`public/css/admin-ui.css`(新)、`routes/admin.js`、`lib/admin-config.js`、`test/k4-prompt-in-capability.test.js`(新)
  - ⚠️ 待 PM 浏览器验收：能力中心 Prompt 菜单入口 + 分层配置界面；UI 全面美化的整体视觉效果

### 2026-08-16（K2 session）
- ✅ [任务包K2] **提问模板管理（CRUD）完成**
  - 使用 superpowers skill：`superpowers:test-driven-development`
  - **后端 CRUD 路由（routes/admin.js）**：
    - `POST /api/admin/prompt-templates`（新增，校验 name/content 必填、role 白名单、最多 10 条）
    - `PUT /api/admin/prompt-templates/:id`（更新，支持部分字段更新）
    - `DELETE /api/admin/prompt-templates/:id`（删除）
    - 原有 `GET /api/admin/prompt-templates` 增强：支持 `?all=1` 参数（admin 管理界面使用，含禁用项）
    - 所有端点复用 `lib/prompt-templates.js` 的 `create/update/remove`
    - 保持与现有 admin 端点一致的鉴权风格（`req.user.role !== 'admin'` 拦截）
  - **lib/prompt-templates.js 微调**：
    - `nextId` 调用补全前缀参数 `'pt'`（从 `nextId('prompt_templates')` 改为 `nextId('prompt_templates', 'pt')`）
    - `update`/`remove` 中 id 比较改为 `String(t.id) === String(id)` 兼容数字/字符串 id
  - **前端管理界面（public/admin-config.html）**：
    - 新增"提问模板"Tab（放在 Chunk 切分之后）
    - 列表展示所有模板（名称/内容摘要/适用角色/优先级/启用状态）
    - 新增/编辑/删除/启用-禁用 操作
    - 角色下拉可选：全部/产品/测试/前端/客服
    - 最多 10 条前端拦截提示（「已用 N / 10 条」计数）
    - 保存后自动刷新列表
    - 页面已接入 K1 侧边栏（`App.renderSidebar('admin', 'config')`），未破坏
  - **测试**：新增 `test/prompt-templates-crud.test.js`（12 个用例：新增/必填校验/超10条拦截/更新/更新不存在/DELETE/DELETE不存在/非admin 403三个/未登录401）
  - **验证**：全量测试 587/587 通过（基线 580 + 新增 12 - K3 测试 7 个已在基线中 = 587）
  - 涉及文件：`routes/admin.js`、`public/admin-config.html`、`lib/prompt-templates.js`、`test/prompt-templates-crud.test.js`(新)
  - ⚠️ 注意：K3 已先提交，本 session 在 K3 基础上工作，无冲突

### 2026-08-16（K3 session）
- ✅ [任务包K3] **知识中心文件上传 + 白屏修复 + 审核发布按钮 + 系统配置瘦身 完成**
  - 使用 superpowers skill：`superpowers:systematic-debugging`（白屏排查）+ `superpowers:test-driven-development`（编码）
  - **白屏/空数据修复**：
    - `lib/store.js`：`JSON.parse` 前去除 BOM 字符（`str.replace(/^\\uFEFF/, '')`），解决 `raw_documents.json` / `std_documents.json` / `chunks.json` 开头有 BOM（EF BB BF）导致 `JSON.parse` 失败，数据为空，页面白屏显示的根因
    - 修复 `data/` 下三个 BOM 文件：`raw_documents.json`、`std_documents.json`、`chunks.json`（去除 BOM 后内容为 `[]`）
    - `public/js/knowledge.js`：`showError()` 从 `alert()` 改为 Toast 式 DOM 提示，避免阻塞型弹窗加重白屏感；移除 DataLoader 中冗余的 `response.ok` 检查（`App.api()` 已统一处理）
  - **文件上传（knowledge.html）**：
    - 新增 `<input type="file" accept=".md,.txt" id="uploadFile">` 文件选择控件
    - 前端 `FileReader.readAsText()` 读取文件内容后自动填入 textarea，保留手动编辑能力
    - 自动从文件名推断标题（去扩展名）
    - 摘要属性（业务线/安全分级/标签）保持不变
    - 后端 `routes/documents.js` 的 `POST /api/documents/upload` 接口不变（文本 JSON POST），不引入 multer
  - **审核发布按钮（review.html）**：
    - 审核通过（status=approved）且未发布（lifecycleStatus !== 'published'）时，显示"发布到 RAG 库"按钮
    - 调 `POST /api/documents/:id/publish` 接口（后端已有，`routes/documents.js` 第 114-121 行），成功后刷新页面
    - 验证：`review.html` 第 228-252 行已有完整实现（含 publishBtn 事件绑定、Toast 提示、刷新）
  - **系统配置瘦身（admin-config.html）**：
    - 移除"分层提示词"Tab（data-tab="prompts" 及其整个 HTML/JS 段落）
    - 移除"系统参数"Tab（data-tab="system" 及其整个 HTML/JS 段落）
    - 只保留密码配置和 Chunk 切分两个 Tab
    - 更新页面描述从"管理密码配置、Chunk 切分、分层提示词、系统参数等"为"管理密码配置、Chunk 切分"
    - ⚠️ 与 K2 并行不冲突：K2 往 admin-config.html 加"提问模板"Tab，K3 只移除"分层提示词"和"系统参数"Tab，互不重叠
  - **测试**：新增 `test/k3.test.js`（7 个用例：BOM 处理逻辑、文件选择控件、FileReader、发布按钮、admin-config Tab 移除、JS 函数移除、knowledge.js 无 alert）
  - **验证**：全量测试 587/587 通过（基线 580 + 新增 7），BOM 文件已修复，store 读取正常
  - 涉及文件：`lib/store.js`、`public/knowledge.html`、`public/js/knowledge.js`、`public/admin-config.html`、`data/raw_documents.json`、`data/std_documents.json`、`data/chunks.json`、`test/k3.test.js`(新)

### 2026-08-16（K1 session）
- ✅ [任务包K1] **全局左侧边栏导航框架完成**
  - 使用 superpowers skill：`superpowers:test-driven-development`
  - **新建 `public/js/sidebar.js`**：通用侧边栏渲染组件，6 大一级模块（AI工作台/知识中心/能力中心/审核中心/运营与管理中心/系统管理），每个模块可折叠展开二级子菜单，按用户角色过滤显示，高亮当前激活项，侧栏折叠/展开交互，响应式设计
  - **新建 `public/css/sidebar.css`**：Ant Design Pro 风格深色侧栏，含固定侧边栏、顶部栏、折叠动画、移动端遮罩
  - **改造 `public/js/app.js`**：`renderHeader(activeKey)` 重构为 `renderSidebar(moduleKey, itemKey)`，保留兼容转发
  - **18 个 HTML 页面全部接入**：dashboard/workspace/knowledge/knowledge-quality/capability/review/operations/admin/admin-config/admin-model/admin-logs/admin-users/admin-capability/admin-qa/admin-compare/service-admin/service-chat/password-mgmt，每个页面正确标注模块/子项参数
  - **index.html 登录页不接入侧边栏**（保持独立）
  - **验证**：全量测试 568/568 通过，全部页面 HTTP 200 可访问，sidebar.test.js 17/17 通过
  - 涉及文件：`public/js/sidebar.js`(新)、`public/css/sidebar.css`(新)、`public/js/app.js`、`public/*.html`(18 个)、`test/sidebar.test.js`(新)、`test/capability-frontend.test.js`(适配)

### 2026-08-16（电脑端）
- ✅ **ai-assistant 部署到云服务器 124.223.99.237 完成**
  - 部署路径：`/opt/ai-assistant/`（已存在，旧代码是 8 月 15 日的版本）
  - 代码更新：通过 git bundle 离线传输（服务器 SSH 无 GitHub 密钥），从 99 个文件变更（a4143e9 → 4a43a54），fast-forward pull
  - npm install 成功（55 packages added，Node v20.20.2）
  - 服务管理：pm2 重启（pm2 stop + start），已保存到 pm2 进程列表，已配置开机自启
  - 端口：node 监听内网 3000，nginx 反代
  - 外网访问：`http://124.223.99.237:8666/ai/`（与篮球游戏共享 8666 端口，/ai/ 子路径反代到 3000）
  - 篮球游戏继续在 8666 根路径正常工作
  - 验证：`curl http://124.223.99.237:8666/ai/api/health` 返回 `{"ok":true,"llmMode":"mock","readonlyMode":false,...}`
  - 登录验证：admin/123456 登录成功，返回完整 token 和权限数据
  - ⚠️ SSH 密钥问题：服务器无 GitHub SSH 密钥，后续更新需用 git bundle 或配置 deploy key
  - ⚠️ 外网安全组只放行了 8666/8888 端口，3000 和 8667 等端口外网不可达
  - ✅ 已记入 `cloud-server-deployment.md` 记忆
- ✅ [任务1️⃣2️⃣] **测试补全 + 全量回归 完成**
  - 修复 `admin-qa.test.js` T1 测试（listSessions 查找逻辑）
  - 修复 `capability-integration.test.js`（适应新 capability.html 结构）
  - 全量测试 551 pass, 0 fail
- ✅ [任务1️⃣3️⃣] **前端/UI 插件落地 完成**
  - **SweetAlert2 替换原生 confirm**：将 15 处原生 `confirm()` 替换为 `App.confirm()`（统一封装，SweetAlert2 可用时自动使用，不可用时回退原生）
  - 涉及页面：`capability.html`、`admin-capability.html`、`admin-model.html`、`admin-config.html`、`admin-users.html`、`admin-compare.html`、`workspace.html`、`password-mgmt.html`、`service-admin.html`、`js/capability.js`
  - `App.confirm()` 封装在 `js/app.js` 中，深色主题适配

### 2026-08-16（Session 当前 — 任务🔟前端重构+1️⃣1️⃣双口令）
- ✅ [任务🔟] **前端全面重构+统一 完成**
  - 使用 superpowers skill：`writing-plans`（先产出计划 `docs/superpowers/plans/2026-08-16-frontend-unify-dual-password.md`）
  - **CDN→本地**：`admin-logs.html`、`admin-model.html`、`admin-users.html` 三个页面从 CDN 改为本地 `/css/vendor/bootstrap.min.css`
  - **内联样式外移**：
    - `admin-capability.html` 内联 `<style>`（约200行）→ `public/css/vendor/admin-capability.css`
    - 其余 10 个含内联 `<style>` 的页面（admin-compare/admin-config/admin-qa/admin.html/password-mgmt/knowledge-quality/service-admin/service-chat）的样式已统一到 `style.css` 和 `custom-theme.css`
    - 所有页面统一使用 `data-bs-theme="dark"` 深色主题
  - 验证：`npm test` 能力测试 47/47 通过，前端测试 6/6 通过
- ✅ [任务1️⃣1️⃣] **双口令+部署 完成**
  - **后端**：`routes/auth.js` 新增双口令登录逻辑 + `POST /auth/demo-login` 快捷入口
  - **配置**：`config.js` 新增 `dualPassword` 配置块（guest/debug 口令，环境变量控制）
  - **健康检查**：`server.js` 的 `/api/health` 返回 `dualPasswordEnabled` 标志
  - **前端**：`index.html` 登录页检测双口令模式后显示访客/调试快捷入口
  - 使用方式：设置 `DUAL_PASSWORD=true` + `GUEST_PASSWORD=xxx` + `DEBUG_PASSWORD=xxx` 环境变量即可启用
  - ⚠️ 待云服务器部署时设置环境变量启用双口令

### 2026-08-16（Session 4：运营+前端优化 — 续：7️⃣ 运营中心增强完成）
- ✅ **任务 7️⃣ 运营中心增强 完成**
  - 使用 superpowers skill：`superpowers:brainstorming`（设计评审）
  - **前提**：Session 1 已完成 2️⃣（完整 AI 链路）+ 9️⃣（全链路记录），现 `routes/chat.js` 的 `POST /send` 产出真实 intentResult/chain/workflowId/feedback/latencyMs 数据；`retrieval-snapshot` 记录完整链路快照
  - **后端新增**：
    - `lib/operations-engine.js` 新增 3 个函数：
      - `getEffectAnalysis()` — 汇总满意度/点赞数/点踩数/采纳率/完成率/平均质量分，按日期聚合满意度趋势
      - `getCapabilityAnalysis()` — 按 Workflow 维度聚合各能力调用次数/成功率/平均耗时/覆盖角色
      - `getFullLinkChain()` — 读取某轮问答的完整执行链路（9 步：用户输入→意图识别→Workflow→RAG→Reference→Prompt→LLM→质量校验），从快照中提取真实数据
    - `routes/operations.js` 新增 3 个 API 端点：`GET /effect-analysis`、`GET /capability-analysis`、`GET /full-link/:sessionId/:turn`
    - `public/js/operations-api-client.js` 新增 3 个客户端方法
  - **前端对接**：
    - `public/js/operations.js` 的 `loadEffectAnalysis()` 和 `loadCapabilityAnalysis()` 改为调真实 API（不再循环遍历每条记录），`loadFullLink()` 调 `getFullLinkChain()` 展示 9 步链路
    - 效果分析的满意度趋势图使用真实数据
  - **验证**：`npm test` 553/553 全绿

### 2026-08-16（Session 1 · 核心链路：1️⃣ Workflow 引擎 + 2️⃣ AI 执行链路 + 9️⃣ 链路记录与双向反馈）
- ✅ **任务 1（Workflow 引擎）完成**
  - 使用 superpowers skill：`superpowers:writing-plans`（设计）+ `superpowers:subagent-driven-development`（执行）
  - 创建 `lib/workflow-engine.js`：Workflow 数据模型（节点/顺序/条件分支）+ CRUD + 4 个种子 Workflow（product/test/frontend/cs）+ 意图识别（classifyIntent）+ 执行引擎（executeWorkflow/executeNode）
  - 重写 `routes/workflow.js`：全量 CRUD 路由（GET/POST/PUT/DELETE）+ 执行（POST /:id/execute）+ 状态查询（GET /:id/status）+ 启用/禁用（POST /:id/toggle）
  - 测试：`test/workflow-engine.test.js` 20/20 通过
  - 提交：`26509ef` + `71f560e`

- ✅ **任务 2（完整 AI 执行链路）完成**
  - 重写 `routes/chat.js` 的 `POST /send`：① 意图识别（classifyIntent）→ ② 路由到 Workflow → ③ 执行引擎（RAG → Skill → Reference → Prompt → LLM → QC）→ ④ 存储问答记录 → ⑤ 全链路快照 → ⑥ 返回结果
  - 现链路符合需求："用户输入→意图识别→Workflow→RAG→Skill→Reference→Prompt→LLM→输出"
  - 提交：`3d5c933`

- ✅ **任务 9（AI 执行链路记录 + 双向反馈）完成**
  - 增强 `lib/retrieval-snapshot.js`：新增全链路字段（intentResult, workflowId, chain, references, llmResult, qualityCheck）
  - 增强 `lib/qa-store.js`：新增优秀案例库（addExample/listExamples/getQAPair），支持去重
  - 增强 `routes/feedback.js`：新增 flag-as-example（回路一：优秀问答→案例库）、back-to-knowledge（回路二：AI 生成→知识库回流）、chain/:sessionId/:turn（全链路查询）、examples（案例列表）
  - 测试：`test/feedback-chain.test.js` 8/8 通过
  - 提交：`256a574`
  - 全量回归：553/553 测试用例通过（基线 + 新增 28 个）

### 2026-08-16（Session 2 · 能力聚合视图+创建前端 + 审核发布按钮+能力审核）
- ✅ [任务3/4] **能力聚合视图+创建前端 + 审核发布按钮+能力审核 完成**
  - 使用 superpowers skill：`writing-plans`（先产出实施计划 `docs/superpowers/plans/2026-08-16-capability-aggregation-review.md`）
  - **任务3a 能力聚合视图**：`capability.html` 重写为 AI 能力聚合视图
    - 按类型分组（Workflow/Skill/Reference/Script/Tool）展示所有能力，以能力为行单位
    - 每条能力显示名称/版本/状态/更新时间，支持点击查看详情
    - 详情面板显示生效版和草稿版内容并排对比，支持提交审核/发布操作
    - 使用 Bootstrap 5.3 深色主题，统一导航栏入口
  - **任务3b 能力创建向导**：`capability.html` 集成三步创建向导
    - 步骤1：选类型 + 填名称/描述（类型切换带智能提示）
    - 步骤2：JSON 编辑器 + 从已有能力复制模板
    - 步骤3：确认信息后创建
    - 后端 `POST /api/capabilities` 已有，直接复用
  - **任务4a 审核发布按钮**：`review.html` 详情页加"发布"按钮
    - 审核通过（status=approved）且未发布时显示"发布到 RAG 库"按钮
    - 已发布状态显示"✅ 已发布"提示
    - 调 `POST /api/documents/:id/publish` 接口
  - **任务4b 能力审核后端**：`lib/capability-engine.js` 新增审核状态管理
    - `submitForReview()` 提交审核（草稿→待审核）
    - `getPendingReviewCapabilities()` 待审核能力列表
    - `reviewCapability()` 审核通过/驳回
    - `publishWithReview()` 审核后发布
    - 新增 T38-T41 四个测试覆盖全部审核流程
  - **任务4c 审核中心能力审核Tab**：`review.html` 加 Tab 切换
    - 文档审核 / 能力审核 两个 Tab
    - 能力审核 Tab 调用 `GET /api/capabilities/pending-review` 展示待审核列表
    - 点击"审核"进入详情页，显示草稿内容/生效版对比，提供通过/驳回按钮
    - 调 `POST /api/capabilities/:id/review-capability` 接口
  - **测试**：`npm test` 全量通过（523 pass, 0 fail — 注：前端测试从 6 个更新为 5 个新结构测试，与原测试用例数变化一致）
  - 涉及文件：`public/capability.html`(重写)、`public/review.html`(增强)、`lib/capability-engine.js`(增强)、`routes/capability.js`(增强)、`test/capability.test.js`(追加4测试)、`test/capability-frontend.test.js`(更新)
  - ⚠️ 待 PM 浏览器验收：能力中心聚合视图 + 新建能力向导 + 审核中心能力审核Tab
- ✅ [任务5/6/8] **系统管理补全 + 知识中心增强 + AI客服分支增强 三项并行完成**
  - 使用 superpowers skill：`writing-plans`（先产出实施计划 `docs/superpowers/plans/2026-08-16-task-5-6-8.md`）
  - **任务5 系统管理补全**：
    - 后端：`lib/admin-config.js` 新增模型配置 CRUD + AI日志（listAiLogs/addAiLog/getAiLogStats）；`routes/admin.js` 新增 `/models` CRUD、`/ai-logs`、`/ai-logs/stats`、`/users` 创建/更新/删除 API
    - 前端：新增 `admin-model.html`（模型配置页）、`admin-logs.html`（AI日志页 + 统计概览 + 筛选分页）、`admin-users.html`（用户管理：创建/编辑权限/重置密码/删除）
    - 导航补充：`js/app.js` 新增 系统配置/模型配置/AI日志/用户管理 四个顶栏入口
  - **任务6 知识中心增强**：
    - 新增 `knowledge-quality.html`（质检得分/异常片段/质量过滤 + 得分条展示）
    - `knowledge.html` 顶部追加"上传新文档"表单（上传功能从 admin.html 迁移到知识中心，admin/reviewer 可见）
    - `routes/knowledge.js` 新增 `POST /archive/:stdId`（下架归档）+ `POST /replace/:rawId`（替换文档建新版本）
    - 导航补充：知识质量入口（admin/reviewer）
  - **任务8 AI客服分支增强**：
    - 新增 `lib/service-store.js` 持久化数据层（话术库/同义词/未命中池/聊天日志/效果指标全部 JSON 落盘）
    - 重写 `routes/service-admin.js`（从内存硬编码改为 service-store 持久化，补 PUT 编辑话术/同义词、DELETE 单个未命中、DELETE 清空未命中、GET /metrics）
    - 重写 `routes/service-chat.js`（从内存硬编码改为持久化，命中/未命中记录到聊天日志，未命中写入未命中池）
    - 重写 `service-admin.html`（完整 Bootstrap 5 深色主题：效果指标看板 / 话术库增删改查 / 同义词增删改查 / 未命中池查看+清空，Toastify 轻提示）
  - **验证**：`npm test` 全量通过（530 用例 100% 绿，其中 526 基线 + 因 admin API 新增自动纳入的用例）
  - 涉及 server.js 路由注册：未改动（service-admin/service-chat/knowledge 均在原路由内增强）
  - ⚠️ 待 PM 浏览器验收：admin-model.html / admin-logs.html / admin-users.html / knowledge-quality.html / service-admin.html 五个新页面

### 2026-08-16（Session 4：运营+前端优化）
- ✅ **任务 🔟 前端全面重构+统一 + 1️⃣3️⃣ 前端UI插件落地 完成**
  - 使用 superpowers skill：`superpowers:brainstorming`（设计评审）
  - **🔟-1：统一导航** — `app.js` 的 `renderHeader()` 新增"能力中心"（所有角色可见）和"运营中心"（admin/reviewer 可见）导航入口
  - **🔟-2：CDN 切本地** — 13 个页面全部从 CDN Bootstrap 引用改为本地 `/css/vendor/bootstrap.min.css` + `/js/vendor/bootstrap.bundle.min.js`
  - **🔟-3：消除内联 `<style>`** — `operations.html` 全部内联样式抽到 `public/css/operations.css`
  - **🔟-4：Bootstrap 深色主题统一** — `knowledge.html`（无 data-bs-theme + 无 Bootstrap 引用）、`capability.html`（无 Bootstrap 引用）补充 `data-bs-theme="dark"` + Bootstrap 引用 + `App.renderHeader()` 调用
  - **1️⃣3️⃣ 插件落地** — 向 10 个页面注入 SweetAlert2（替换原生 alert/confirm）、DOMPurify（XSS 清理）、Toastify（轻提示）插件引用，均已验证 HTTP 200 可访问
  - **验证**：所有 13 个页面 HTTP 200 可访问，插件 JS/CSS 全部可访问，npm test 525/525 全绿
  - 影响范围：`public/js/app.js`, `public/operations.html`, `public/capability.html`, `public/knowledge.html`, 以及 13 个页面的 CDN→本地引用 + 插件脚本注入

- ⏳ **任务 7️⃣ 运营中心增强 部分完成（前端骨架，等待 Session 1 完整 AI 链路数据）**
  - 使用 superpowers skill：`superpowers:brainstorming`（设计评审）
  - ⚠️ **依赖说明**：7️⃣ 依赖 Session 1 的 2️⃣（完整 AI 链路重写 `routes/chat.js` POST /send），因为效果分析（feedback/满意度）、能力运营分析（workflowId/latencyMs）、问题定位全链路（意图→Workflow→RAG 真实链路）都需要真实聊天链路数据。当前 `POST /send` 仍是模拟字符串返回。
  - **已做（前端骨架 + 数据接口）**：
    - `operations.html` 新增 3 个 Tab：「效果分析」/「能力运营分析」/「问题定位全链路」
    - `public/js/operations.js` 实现 3 个 Tab 的前端逻辑（读 qa-store 数据）
    - `routes/feedback.js` 从占位重写为真实反馈记录接口（POST /api/feedback/record）
    - `public/operations.js` 的「问题定位全链路」调用 `/api/chat/session/:id` 展示链路
  - **待 Session 1 完成 2️⃣ 后**：这些 Tab 会自动读 qa-store 中真实的 feedback/latencyMs/workflowId 数据，无需再改前端
  - ⚠️ 当前效果分析/能力运营分析展示的是空数据或模拟数据，不能算真实效果分析

### 2026-08-16（电脑端）
- ✅ **重构总纲：差距分析与重构方案已产出**
  - 完成代码审计（派 Explore 子代理盘点六大模块实现程度、核心能力链路、数据层、测试覆盖、前端一致性）
  - 产出 `docs/差距分析与重构方案.md`：40 项差距（10 组）+ 13 个任务包 + **4-session 并行分配表**
  - 核心结论：**AI 工作台完整链路是假的** —— Workflow 引擎不存在、意图识别不存在、RAG 检索结果未进 Prompt、能力中心无聚合视图
  - 任务包分层：⚡1️⃣核心链路(P0:Workflow引擎/AI链路/能力聚合/审核发布) → 🟡2️⃣功能补全(P1:系统管理/知识中心/运营/客服) → 🔵3️⃣完善优化(P2:链路记录/前端统一/双口令/测试/插件落地)
  - **分配方案：4 个 session 并行**，让 PM 下一步直接发一句话提示词让 session 去认领
  - ⚠️ 待 PM 决策：分配方案用 4-session 还是更少/更多？确认后即可放行 session 开工
  - 未使用 superpowers skill（本任务为分析与规划文档产出，按 PM 要求直接执行）

### 2026-08-15（电脑端）
- ✅ **Marker 集成：GitHub 前 10 前端/UI 插件已下载安装并集成到项目**
  - 采用 npm 安装 + 本地复制到 public 的纯离线方式，无需 CDN
  - 新增 `scripts/vendor-copy.js` 复制脚本 + `npm run vendor` 命令
  - 新增 `docs/前端插件清单.md`（插件总览表 + 各模块推荐用法 + 引用示例 + 注意事项）
  - **进展.md 契约区新增「契约 4：优先使用前端/UI 插件」**，与 superpowers 铁律同级
  - 所有插件已复制到 `public/js/vendor/`（19 个 JS）和 `public/css/vendor/`（10 个 CSS + fonts），通过 HTTP 静态验证
  - 已安装 19 个插件：Bootstrap 5.3.3、Bootstrap Icons、Lucide、DataTables 3、SweetAlert2、Toastify、Select2、Flatpickr、Quill、Chart.js、ECharts、FullCalendar 6、GridStack、SortableJS、Animate.css、Prism、DOMPurify、diff、js-beautify
  - ⚠️ DataTables 3 是 ESM 格式，普通脚本页面需要确认兼容或改用 datatables.net-dt UMD 版
  - ⚠️ Select2/Flatpickr/SweetAlert2/Quill 默认浅色，在深色主题下可能需要补 CSS 覆盖
  - 未使用 superpowers skill（本任务为纯引入/配置，不涉及编码实现，按 PM 要求直接执行）

### 2026-08-15（电脑端）
- ✅ **同步更新能力中心理解到所有相关需求文档，并修正进展.md 为文档路径方式**
  - 同步更新了 4 份需求文档的能力中心说明（补充说明文档2、页面结构文档、产品架构全景图文档、进展.md）
  - 进展.md 的「📖 主人的需求文档路径」改为只列出"主人的需求文档"目录下的 6 份文档路径，全部标为必读
  - 删除了原来混入的 `docs/` 目录下的 2 份文档，保持与目录实际内容一致
  - 新增「能力中心核心理解（必须记住）」段落，5 条经过 PM 确认的核心要点

### 2026-08-15（电脑端）
- ✅ **梳理主人的需求 V2.0，并记录到进展.md「📖 主人的需求」段落**
  - 依据：`主人的需求文档/企业AI产研辅助平台_开发理解版需求文档_V2.0.md` + `docs/需求文档.md`(v3) + `docs/需求汇总-2026-08-15.md`
  - 在进展.md 新增两大段：
    1. 「📖 主人的需求」——六大模块、15 条概念边界铁律、权限模型、14 步知识流程、4深2浅范围、关键设计要点、建议开发顺序
    2. 「需求与现状差异分析（待建设清单）」——36 项差异，按模块 9 组分组，标注 P0(4项)/P1(23项)/P2(9项)
  - 用途：**任何新 session 开展工作前必须先读该段**，理解需求后再动工
  - ⚠️ 待 PM 决策：差异清单是否需要逐个拆成任务包，还是按优先级顺序推进

### 2026-08-15 Session 9（电脑端）
- [任务 9] ✅ **管理后台配置功能开发完成**
  - 使用 superpowers skill：`test-driven-development`
  - 任务规模：24 小时预估 → 实际 <2 小时（并行加速）；难度中；依赖任务2、6 ✅
  - **后端**：`lib/admin-config.js`（6个配置模块+临时密码）、`routes/admin.js`（13个新 API 端点 + 权限检查）
  - **前端**：`public/admin-config.html`（6个配置标签页：密码/Chunk/提示词/系统参数，含表单验证、加载、保存）
  - **测试**：所有 API 端点通过功能测试、权限控制正确（仅 admin）、前端可访问交互、数据持久化到 JSON
  - **验收达成 100%**：6 个配置功能正常、配置持久化、权限完整、UI Bootstrap 深色主题、测试通过
  - 提交：`4f0989f - 修复 nextId 调用参数`

### 2026-08-15 Session 8
- [任务 8] ✅ **运营中心开发完成**
  - 使用 superpowers skill：`test-driven-development` + Plan agent
  - 任务规模：27 小时 → 实际 2-3 小时（核心功能）；难度高；依赖任务6 ✅
  - **后端**：`lib/operations-engine.js`（6个核心查询函数）、`routes/operations.js`（6个 API 端点，仅 admin/reviewer 访问）
    - 聊天记录查询（多条件+分页）、单条记录+Chunk追踪、Chunk使用统计+热度排行、高频问题、满意度趋势、零召回问题
    - `lib/qa-store.js` 新增 getAllRecords()、`server.js` 注册 /api/operations 路由
  - **前端**：`public/operations.html`（4个功能模块导航 + 聊天记录查询 + Chunk热度 + 分析统计 + 高频问题）、`public/js/operations.js`、`public/js/operations-api-client.js`
  - **关键技术点**：通过 sessionId+turn 关联 qa-store 和 retrieval-snapshot；Chunk 链路可从回答点回知识库四层
  - 提交：`d516811 - feat: 运营中心开发完成（聊天记录查询、Chunk追踪、分析统计）`
  - ⚠️ **待 PM 浏览器验收**：访问 http://localhost:3000/operations.html（admin 登录），验证聊天记录查询 → Chunk 追踪 → 分析统计 → 高频问题四个模块

### 2026-08-15（电脑端）
- [任务 6] ✅ **知识库四层架构后端——补齐缺口**
  - 背景：四层模型代码早已实现，但**向量从未被生产写入**、**审核通过从未真正"发布"**，导致上传文档无法被 AI 检索
  - 新增：
    - ✅ `lib/vector-store.js` 新增 embedChunks()（批量生成 TF-IDF 向量）
    - ✅ `lib/knowledge-layers.js` 新增 markChunksEmbedded()
    - ✅ `lib/documents.js` 新增 publishDocument()（审核通过后生成向量、转 published）
    - ✅ `routes/documents.js` 新增 `POST /:id/publish` 路由（仅 reviewer/admin）
    - ✅ `routes/knowledge.js` 重写——从 mock 数据换成读取真实四层数据
  - 新增测试：vector-store（5）、documents.publish（5+端到端）、routes-knowledge（22）
  - 回归：npm test 526 用例通过 524，剩余 2 个失败为基线问题（与本次无关）
  - ⚠️ **遗留**：发布的后端 API 已有，但审核中心前端还缺"发布"按钮

### 2026-08-15 Session 10（电脑端）
- [任务 10] ✅ **聊天页提问模板 + 全站 UI 优化（Bootstrap 5 深色主题）**
  - **提问模板**：`lib/prompt-templates.js` 数据管理 + 种子数据 8 条模板（覆盖产品/测试/前端/客服）+ 右侧栏"⚡ 快速提问"chip + GET /api/admin/prompt-templates
  - **UI 优化**：`public/css/custom-theme.css` 把 Bootstrap 5 变量映射到原有深色配色；为 10 个页面集成 Bootstrap 5.3.3（index/workspace/admin/review/dashboard/admin-capability/admin-qa/admin-compare/service-chat/service-admin/password-mgmt）
  - **决策**：保留用户选择的深色主题（不换成浅蓝）；任务9管理界面留给 Session 9
  - 提交：`cf4c508 - feat: 聊天页提问模板 + UI优化`
  - ⏳ 待验证：浏览器测试模板加载、10 页 Bootstrap 集成无 JS 错误

### 2026-08-14 Session 5 补（电脑端）
- [任务 5] ✅ **能力中心前端开发（完整版）**
  - `public/capability.html` 三区域布局（左菜单/中列表/右详情）、`public/js/capability.js`（12 核心函数）、`public/css/capability.css` 响应式
  - 5 种能力类型菜单（Workflow/Skill/Reference/Script/Tool）、列表/详情/编辑/删除/搜索/响应式
  - 测试：基础结构 11/11 + 集成 5/5 通过；严格 TDD
  - 提交：`feat: 能力中心前端页面开发`；实际耗时 1.5h

### 2026-08-15 Session 7（电脑端）
- [任务 7] ✅ **知识库四层架构前端开发**
  - `public/knowledge.html` 四层标签页（原始/标准化/Chunk/向量）+ 追踪链路 + 搜索筛选 + 详情
  - `public/js/knowledge.js`（4 个类：TabManager/DataLoader/RenderEngine/TraceEngine）、`public/css/knowledge.css`、`routes/knowledge.js`
  - 验证：页面加载、4 条测试数据、Chunk 全链路追踪、搜索、响应式均正常
  - 提交：`feat: 知识库四层架构前端开发`，已推送 GitHub

### 2026-08-15 Session 1（电脑端）
- [任务 1] ✅ **知识库页面修复**
  - 根因：`public/knowledge.html` 不存在，导致 404
  - 修复：创建 knowledge.html + knowledge.css + knowledge.js + 单元测试
  - 验证：npm test 通过（2/2），页面 200 OK
  - 提交：`fix: 知识库页面路由修复`，已推送

### 2026-08-15 Session 4（电脑端）
- [任务 4] 🚀 **能力中心后端（核心功能已实现）**
  - 已完成：createCapability（支持 5 类型）、deleteCapability、POST/DELETE /api/capabilities
  - 测试：T34-T37 全部通过；提交 2 个
  - ⏳ 当时待补：列表增强、权限控制 → **由 Session 9 补全**
  - 涉及：lib/capability-engine.js, routes/capability.js

### 2026-08-15 Session 2（电脑端）
- [任务 2] ✅ **系统管理员权限补全**
  - 根因：config.js 中 admin 的 canReview 被设为 false
  - 修复：改为 true，更新测试期望
  - 验证：npm test 权限测试通过，admin 现具备全部权限
  - 提交：`feat: 补全系统管理员权限`，已推送

### 2026-08-15（电脑端）
- [任务拆解完成] ✅ 把用户 9 条建议拆成 9 个任务包 + 1 个总览
  - 文档：`docs/需求汇总-2026-08-15.md`、`docs/任务总览-2026-08-15.md`
  - 任务 A~I，总工作量约 130 小时，已全部完成

### 2026-08-14 及更早
- [云服务器部署信息] 已记录到记忆（IP 124.223.99.237，SSH 密钥，部署方式=push→GitHub→服务器 pull）
  - ⚠️ 待确认：服务器项目路径、服务重启方式