# AI 辅助工具项目 · 进展记录

## 2026-08-12 03:25（电脑端）
- 「拆闸门」阶段 1 后续：独立审查 + 9 个补充测试落地
  - 派了第二个子代理做独立审查，**0 阻塞 4 条建议**
  - 采纳了其中 3 条，全是覆盖契约的关键点：
    - **8 状态全覆盖测试**：`LIFECYCLE_TO_OLD` 表（draft/qc_failed/pending/approved/published/need_review/rejected/archived → 旧 3 值）是前端兼容层的核心契约，**任何 typo 都会让前端 UI 拿错状态**（且类型系统抓不到，全是字符串）。逐状态驱动 std、断言 view.status 和 view.lifecycleStatus 各一遍
    - **纯草稿兜底分支测试**：raw 已 markReady 但还没 createStdVersion 时，view 应该返回 lifecycleStatus='draft'、chunks=[]。补足"无 std"路径覆盖
    - **`LIFECYCLE_TO_OLD` 上方加注释**：明示"这是兼容层，typo 是 silent bug"—— 警告未来改动的人
  - 第 4 条建议（孤儿 `currentStdId` 防御注释）推迟到阶段 2+ 路由接入时再补——目前 `deleteStdCascade` 已保证不变量
  - 测试从 262 → **271 全过**（+9 个），0 skip
  - 数据隔离验证：`data/raw_documents.json` / `std_documents.json` / `chunks.json` 物理上不存在（withTempDataDir 正确隔离到 os.tmpdir()），新四表在真 data 目录里零落点

## 2026-08-12 03:10（电脑端）
- 「拆闸门」计划 **阶段 1 完成**：四层模型已经能用了，先把最常用的"看一个文档完整信息"这条路径开放出来
  - 阶段 1 是个**纯函数任务**：在 `lib/documents.js` 顶部加一个 `getDocumentView(rawId)`，不改现有任何 API。这样后面把上传、审核、列表这些老入口一个一个搬到新结构时，每改一个都还能用上 `getDocumentView` 当真相来源
  - 它做的事情：拿到一个 raw 文档 id，自动拼出它的最新状态、切片数、片段列表，所有字段一张表全在一处。一行代码完成"raw 元数据 + 当前生效 std + 全部 chunks + 状态映射"
  - **状态映射**对老系统很关键：四层模型里 std 有 8 个状态（draft / pending / approved / published / need_review / qc_failed / rejected / archived），但老上传/审核链路只认 3 个值（pending / approved / rejected）。新视图同时输出两个字段：`lifecycleStatus` 给新系统用（精确）、`status` 给老链路用（兼容）。这样将来切流量时不会因为"前端还在读 status"就突然崩
  - **chunks 投影出去不带权限判据字段**（bizLine / securityLevel / status 全部不带）—— 这是上次第 7 步"未审核文档被搜到"事故的教训：调用方读到这些字段就会下意识自己做权限判断，做错就是越权。引擎层过滤是唯一安全的地方，调用方过滤只是优化
  - 严格 TDD：先写 3 个测试看到红（`getDocumentView is not a function`），再写实现看到绿
  - **测试 262/262 全过，0 个 skip**（基线 259 + 新增 3）。`git diff` 只动了 `lib/documents.js`（+85 行）和新增 `test/document-view.test.js`，现有 5 个导出函数一个没改
  - 踩坑记录：任务描述里写"建 std → 发布 → 加 chunks"这个顺序是**错的**（已发布版本不能再加片段，这是 I6 安全约束），我按"建 std → 加 chunks → 发布"写测试，并照此推进
  - 下一步：阶段 2 —— 把 `upload()` 接到新结构上（用户上传后落到 raw 文档，触发解析 + 创建 std 草稿）。这一动比阶段 1 危险，因为 `upload()` 是真实写入路径，要保证老测试还过的同时新数据流向新表

## 2026-08-12 02:00（电脑端）
- 第 8 步收尾：**安全漏洞和契约漏洞全部修掉，259 个测试全过**
  - 上一条 00:29 写的「228 个测试全过」其实藏着一个 **测试全绿也发现不了的安全漏洞**：已发布版本可以偷偷追加审核员从没看过的内容，立刻进检索。228 个测试、六条不变量、自检全过，**所有绿灯都没发现**。是独立审查代理主动去攻击不变量才捞出来的
  - 漏洞根因：四层契约只规定了下层"继承什么"，没规定"什么时候不许继承"。审核员批的是 std 的正文，但切片创建函数对所属 std 的当前状态毫无校验，结果发布后还能继续往里面塞片段，新片段一出生就 `published`
  - 修法：所有"写"操作都加守卫。`createChunks` 在已发布/已归档等状态下直接 409 拒掉，要改就新建版本；`createVector` 对归档版本拒掉（终态版本向量永不参与检索），但允许给已发布/复审中版本补建或换模型
  - 契约本身也被找出三个真漏洞，全补了：
    - 状态机补两条流转：草稿可以废弃（`draft → archived`）、可以撤回送审（`pending → draft`），这两个日常动作原本无路可走
    - **版本回滚的语义定下来**：不是让旧版本复活，而是 `rollbackToVersion()` 拿旧版 content/params 建一个新版本（draft）走正常审核。版本号只增不减，审计上永远能看出"当前第几版、每版活过哪段时间"——同 git 用 revert 而不是 reset 的理由
    - 加了第 7 条不变量 I7：`raw.currentStdId` 必须等于该 raw 下 `isCurrent` 的 std 的 id，否则影响预览的 reprocess 路由会算错
  - 审查还捞出来一些不留神会掉坑的：
    - **`listAll` 返回 store 缓存里的活对象**，外部改了 vec 数组字段，下次落盘就被持久化——直接绕过 `setStdStatus` 那道闸门。已统一改成浅拷贝（数组字段也拷）
    - **I6 自检是恒真死代码**：`checkInvariants()` 里遍历 `listRetrievableVectors()`（已经按"status∈RETRIEVABLE && isCurrent"过滤过）再断言同一谓词，永远为真，从未真正报过错。改为**自下往上核对**（回查上游 std 是不是真的可检索）
    - **迁移脚本半截失败会静默丢数据**：`applyOne` 返回后 push 登记 raw id，raw 已落盘但后续步骤失败时这条没进 `createdRawIds`，不会被回滚。改成立刻登记。同时 `migrate()` 加了**显式闸门**——默认 require 时不执行、命令行不带 `CONFIRM_MIGRATE=1` 也只打警告不动手，因为当前上传/审核/检索还在读旧表，跑迁移会让应用变空知识库
  - 代码里有一段来源不明的字面量（形似 `502976064786_AWS_us-west-2`），改回 `${id}` 模板。全项目扫了一遍，**别处没有类似异物**（AWS 账号形状、AKIA/sk- 密钥前缀、us-west/区域名都零命中）
  - **第 12 步的存储问题提前拍板**：审查代理拿真实语料实测，13 个文件 → 373 片段、词表 **11093 维**，按现在稠密存的写法，单文件约 100MB、累计写盘十几 GB、分钟级。本步备好 `encoding: 'dense'|'sparse'` 字段、`createVectors`/`createVectorsBatch` 批量接口、vectors.json 不缩进。第 12 步把 `vectorize` 改稀疏即可（vectors 表现在空的，切换零成本）
  - 契约文档 [docs/技术方案-四层模型.md](ai-assistant/docs/技术方案-四层模型.md) 同步更新（I7、新流转、rollbackToVersion、批量接口、impactOf 行为约定、稀疏编码决策）
  - **259/259 测试通过**（基线 161 + 新增 98）。三个新测试文件单独跑前后 `data/documents.json` 哈希未变——隔离干净
  - ⚠️ **未做、留给下一批**：
    - 旧 `documents.test.js` 还在往真数据里写（这是上一条 00:29 就提过的"演示数据 20 条 19 条垃圾"的源头）。建议下一批先把它改了
    - `lib/documents.js` / `routes/*` 还没接到新结构，所以闸门现在不能拆
    - 第 12 步的稀疏向量切换

## 2026-08-12 00:29（电脑端）
- 第 8 步核心部分完成：**四层文档模型的底座建好了**（原始文档 → 标准化版本 → 知识片段 → 向量）
  - 这一层做完，后面这些才有可能：重新加工先出草稿再对比生效、换模型做效果对比、删除前看影响预览、一句答案能查到出处
  - 测试从 161 个涨到 **228 个，全部通过**
- 六条"绝不能破"的规则都用测试锁死了，其中两条是**上次真出过事故的**：
  - 一份原始文档同时只能有一个生效版本（否则 AI 会同时命中新旧两版，答案自相矛盾）
  - 同一个片段每个模型只保留一份生效向量（否则同一段话被搜出来两次，挤掉别的答案）
  - **权限判据（业务线/密级/状态）每层强制从上层继承，调用方传什么都不认** —— 第 7 步"未审核文档能被搜到"就是这里漏了一个字段引起的，现在从根上堵住了
  - 发布/归档时，下面的片段和向量状态跟着一起变，一个都不许漏
  - 删除文档会连带删掉它的全部版本、片段、向量，不留半截数据
  - 只有"已发布"和"待复审"的内容能被搜到
- 状态流转做了**全组合遍历测试**：8 个状态两两配对共 64 种走法，其中 52 种非法走法全部被拦住（比如草稿不能直接发布、归档了不能改回已发布）
- 影响预览已经能算出"这个操作会影响 N 个版本 / N 个片段 / N 份向量"，并提示有多少向量会立刻退出检索
  - 「其中 X 个片段被引用过 Y 次」这半句要等第 14 步的检索快照做完才有数据，现在会明确显示"引用统计暂不可用"，不会报错
- 旧数据搬迁脚本写好了，拿真实数据在**沙箱里试跑过三遍**：18 篇全部搬迁成功、重复执行不会产生重复数据、自检全绿。**脚本还没有对正式数据执行**，等您确认
- ⚠️ **待你决定（1）：旧数据什么时候正式搬迁**。搬迁会把 `data/documents.json` 改名归档，代码不再读它。建议等下一批（把上传、审核、检索接到新结构上）做完再一起执行，中间少一次来回
- ⚠️ **待你决定（2）：现在的演示数据几乎全是测试垃圾**。`data/documents.json` 里 20 条记录，只有 1 条是真文档（test.md），另外 19 条是历史上每次跑测试自动塞进去的空壳记录（无标题、正文是一句重复的占位文本）。原因是一个老测试会往正式数据里写东西，跑一次多一条
  - 选 A：先不动，等第 19 步做前端演示数据时统一重建
  - 选 B：现在就清掉那 19 条空壳，并把那个老测试改成不碰正式数据（我倾向 B，不然搬迁过去的也是垃圾，而且以后每跑一次测试就多一条）
- 另外发现 `npm run reset`（一键恢复初始数据）指向的脚本文件是空的 —— 部署方案里"真实模式一键恢复"这个功能目前是个空壳，第 15 步部署前必须补上
- 下一批：把上传、审核、检索这三条现有流程接到新结构上（现在新旧两套并存，老的还在正常跑，没有停机风险）

## 2026-08-11 22:50（电脑端）
- 五条新需求全部评审完并写进文档，需求文档升到 **v3**：`docs/需求文档.md`
- **AI 客服走关键字匹配** —— 这个方向对，而且理由比想象的硬：客服问题高度收敛、关键字可解释可运营（命中哪个词运营看得懂，向量"为什么没召回"运营查不了）、错答成本极高（对客户说错话是投诉赔付）、能硬绑定话术。**一个平台里两种检索模式并存，是这个项目最好的技术对比素材** —— 说明你不是"学了 RAG 就到处用"，而是按场景选方案，这比多做三个页面值钱
  - 三个死穴已在方案里解掉：① 配**运营可维护的同义词表**（客户说"钱啥时候到"不说"退款"，没这张表大面积零命中，这是成败关键）② 多关键字按"命中词权重和"排序，罕见词权重高 ③ 建**未命中问题池**，这是客服知识库唯一的成长路径
  - 好处是效果可量化：改完同义词表重跑，"命中率 62%→87%、转人工 38%→13%"，这个数字比任何描述都直观
- **召回可观测 + 反向修复** —— 这是五条里最有价值的一条，它决定这个项目是"做了个 RAG"还是"做了个能运维的 RAG"。大多数人做到"能答"就停了，做不到"答错了能查、能修、能验证修好了"
  - 每次回答留完整**检索快照**：问题 → 拆词 → 权限过滤掉了哪些为什么 → 召回哪些片段各多少分 → 实际用了哪几条 → 输出
  - 三种修复：改原文 / 调参数重新标准化 / 删除，都能看到影响链路
  - ⚠️ 两个安全设计**必须做**，否则这功能会变成事故来源：① 执行前显示影响预览（"将影响 37 个片段，其中 12 个过去 7 天被引用 45 次"）—— 不然管理员调个参数把整条业务线搞坏了都不知道 ② 重新加工**先出草稿版本**，新旧切分结果并排对比 + 拿历史真实问题重放验证，确认才生效
  - 这正是四层模型里"1:N 版本"真正发挥作用的地方 —— 当初坚持留版本，是为了这一刻
- **能力中心升级为真能配置**（你否掉"浅做"是对的，Skill/Prompt 就是"生成质量好不好"的直接原因）。配套护栏：改动先存草稿 → 拿真实需求试跑 → 新旧输出并排对比 → 满意才发布 → 保留版本可回滚 → 全程留痕
  - 补偿工作量：**AI 运营收窄**，只深做 RAG检索分析 + 使用数据。范围从 3深3浅 改为 **4深2浅**
- **部署方案我重做了一版** —— 原方案想窄了，把"你"和"访客"当成同一个身份。你要真实删改调试是刚需，**自己不能真实测就不知道系统好不好用**：
  - **一道门，两把钥匙**：同一个链接同一个输密码页，但两个口令。访客口令→演示模式（写操作能点、有影响预览、不落库）；你的调试口令→真实模式（全部真实生效）。仍然无登录页，仍然可切角色
  - 顺手解掉三个隐患：只有一个口令就等于把删库权限发给每个拿到链接的人；你调试到一半有人来看 → 真实模式给你「一键恢复初始数据」；误操作 → 真实模式常驻橙色横幅
  - ⚠️ 安全边界写进文档了：共享口令+无登录 = 所有访客同一身份，这道门**防蹭不防攻击**，服务器上不能有任何真实企业数据（目前全是模拟数据，没问题）
- **另外补了一个你没提但你真正需要的功能：「回答对比调试」**。光有写权限不够 —— 改完文档靠记忆比对上次的回答根本没法用。做法：存一组测试问题 → 跑一遍存快照A → 改东西 → 再跑存快照B → 并排对比哪些召回变了、哪些回答变了、变好还是变坏。这一个工具同时服务三件事：重新加工的回归验证、Prompt 改动试跑、同义词表效果验证
- **落地页会加"三步体验"引导** —— 演示项目最大的失败是访客点进来不知道看什么就走了
- 剩余步骤重排为 16 步，下一步仍是：四层文档模型重构（其他所有功能都压在这层上）

## 2026-08-11 21:40（电脑端）
- 需求 v2 评审完成，已整理成正式文档：`docs/需求文档-v2.md`（这就是您要的"看了能丰富产品经验"的那份，可直接读）
- **您这版需求专业度上了一个档**，四层文档模型 + "原始文档必须保留"是最值钱的两点 —— 这是区分"用过 RAG"和"想过 RAG 怎么运维"的分水岭
- 评审发现 3 处会真出问题的，已在 v2 里改掉：
  1. **质量检查放在第 11 步太晚**：第 9 步一写进向量库，AI 就能搜到了，等第 11 步发现不合格已经污染两步。改成前移 + 拆两道（文档级、片段级），都在写入之前
  2. **只有上线没有下线**：这是企业 RAG 最常见的翻车点 —— AI 引用了 2023 年已废弃的规则，因为旧文档从来没人下架。补了第 14 步「下架/替换/归档」+ 有效期自动提醒复审
  3. **去重位置偏早**：切分前只能按段落去重，但真正污染检索的是重复的**片段**（同一段话在 5 份文档里都有，搜出来 5 条一样的，挤掉了别的答案）
- 另外补强了 6 处：表格/图片解析规则、解析失败状态、四层对应关系改 1:N、权限表补「业务线 × 密级」交叉维度、技术管理员正文脱敏、架构图补权限过滤和反馈回路
- **本期决定**（已记入 v2 决策表）：
  - 演示访客限制为仅公开级 + 页面加「切换身份」按钮 —— 这样权限隔离才演示得出来
  - 六大模块 **3 深 3 浅**：深做 AI工作台 / 知识中心 / 审核中心（这三个连起来是完整闭环），AI能力中心和AI运营做只读展示，系统管理只留配置页
  - 审核是两道业务关卡：① 文档上传后审核才生效 ② 优秀问答对入库需审核。关卡采用「原文 ↔ 加工结果」并排对照视图 —— 既满足您的要求，又能发现"清洗把关键表格洗坏了"这类只看 Word 原文发现不了的问题
- ⚠️ **提醒一个风险，请留意**：优秀问答对入库是**全系统唯一可能越用越差的地方**。AI 生成的内容一旦被审核员顺手点通过就变成"知识"，以后反复引用；要是当初那条是 AI 编的，这个错误会永久固化而且越用越像真的。v2 里定了四条防护：来源标记「AI生成+人工确认」、检索权重低于原始文档、引用时答案里标注来源性质、**审这类内容的必须是业务负责人**（要判断内容对不对，不只是格式对不对）。第 14 步实现时会落地
- **计划有调整**：四层文档模型需要先重构，插在写 Skill 之前。原因是 Skill 和工作流都要读知识层，等它们写完再改等于推倒重来。好消息是第 6、7 步的检索引擎不用重写（TF-IDF、权限过滤这些跟分几层无关），重构的是存储和流转部分
- 剩余步骤已重排为 12 步，下一步开工：四层文档模型重构

## 2026-08-11 20:10（电脑端）
- 第 7 步完成：**权限已经接到检索上了**
  - 交易线的人搜"会员积分"→ 一条都搜不到
  - 普通产品经理搜到的内容里，不会混进"绝密"级文档
  - 还在审核中的文档，不会被任何人搜到
  - 客服只能搜到对外公开的内容
- 代码审查抓到 3 个**会真的出事**的问题，都已修掉：
  1. **检索功能读错了文件**，上线后知识库会永远是空的 —— 上传流程存到 A 文件，检索却去读 B 文件（B 文件根本没人写过）。原来的测试之所以"通过"，是因为测试自己造了个假的 B 文件。
  2. **权限检查形同虚设**：检索函数收了"当前用户"这个参数，但代码里从没用它，安全全靠调用方自觉。第 9 步一接进去就是全员越权。已改成检索引擎自己必查一遍。
  3. **未审核文档能被搜到**：建索引时漏拷了"审核状态"字段，导致状态检查悄悄失效。
- 另外修了测试互相干扰导致的随机失败（多个测试文件同时抢同一份数据文件）
- 161 个测试全过，连续两轮稳定
- ⚠️ **需要您决定**：演示用的 guest 账号目前**能看到全部内容**（包括绝密级）。
  - 选 A：保持现状 —— 访客点开云端链接能随便逛，看得到全部功能
  - 选 B：限制成只看"公开"级 —— 访客会亲眼看到"这份文档你无权查看"，**这个项目最值钱的卖点（权限隔离）才演示得出来**
  - 我倾向 B，但可以在演示页放个"切换身份"的按钮，让访客自己切成产品经理/客服来对比效果，两边都不损失
- 准备进入第 8 步：写 4 个"技能包"（PRD / 测试用例 / 前端组件 / 客服问答）+ 提示词组装 —— 这是决定"AI 生成出来的东西专不专业"的关键一步

## 2026-08-11 18:30（电脑端）
- 第 6 步完成：知识片段转成向量、按意思能找到
  - 用 TF-IDF + 余弦相似度，零依赖
  - 13 份文档 373 个片段建索引，搜"退款流程" / "会员积分" 都能命中正确业务线
- 修了 5 个代码审查问题：rerank 保留原分、按 query 长度归一加分、停用词不过滤排序、tokenize 不再重复实现、阈值用 config
- 126 个测试全过
- 准备进入第 7 步：把"权限"接到检索上 —— 交易线的人搜不到会员线的文档、客服只看到对外公开的内容

## 2026-08-11 17:05（电脑端）
- 第 5 步完成：文档可以**上传、审核、设定安全分级**了
  - 流程：管理员上传 → 状态"待审核" → 审核员通过 → 进入 RAG 库
  - 已用 13 份模拟文档跑通：审核通过后产出 373 个知识片段
  - guest 演示账号被**后端**拦截，演示链接发到云上不会被乱改
- 修复 2 个代码质量问题：跨文档重复内容会去重、关键词里"款审"这种切错的词被过滤
- 第 5 步代码已派子代理独立审查中
- 准备进入第 6 步：把知识片段转成向量、让 AI 能"按意思找到知识"（这是 AI 工具"理解需求→找对应知识"的核心）

## 2026-08-11 10:00（电脑端）
- 项目计划已批准
- 阶段零（工程基建）和阶段一（基础框架）已完成
- 见 `d:\temp\ai-assistant` 仓库与 `docs/项目说明文档.md`（待第 15 步出）
